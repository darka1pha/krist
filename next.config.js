/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		remotePatterns: [
			{ hostname: 'swavffutisgneenuzdoh.supabase.co', protocol: 'https' },
		],
	},
}

module.exports = nextConfig
